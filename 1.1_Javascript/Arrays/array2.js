//PROPRIEDADES DO ARRAY - Construtor, prototype, length

//CONSTRUTOR
/*Construtor é uma função nativa da linguagem que faz referência
a função que cria o objeto array*/

var arr = [3, "a", 64, "d", "e"]; //Foi criado um objeto array

window.onload = function(){
    //alert(arr.construtor); //Alerta function Array() {[native code]} 
}

//PROTOTYPE
/*Trata-se de uma propriedade comum a todos os objetos*/

//LENGHT - Retorna a quantidade de elementos de um array

var x = arr.length;
console.log(x);
